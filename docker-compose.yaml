version: '3'

services:
    pfm-service:
        container_name: pfm-service
        build: .
        command: "python manage.py runserver 0.0.0.0:8000"
        volumes:
            - ./src/:/code
        env_file:
            - .env
